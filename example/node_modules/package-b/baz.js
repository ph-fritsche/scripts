module.exports = {
    description: 'Outputs "baz"',
    options: {
        loud: {description: 'UPPERCASE'},
    },
    run(params) {
        process.stdout.write(params.options.loud ? 'BAZ' : 'baz')
    },
}
