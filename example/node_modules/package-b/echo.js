module.exports = {
    description: 'Outputs every argument in a new line',
    variadicArgs: {id: 'args', description: 'The lines to be echoed'},
    run(params) {
        params.variadic.forEach(l => process.stdout.write(`${l}\n`))
    },
}
