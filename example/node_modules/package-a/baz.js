const { stdout } = require('process')

module.exports = {
    description: 'Outputs "baz"',
    options: {
        loud: {description: 'UPPERCASE'},
    },
    run(params) {
        stdout.write(params.options.loud ? 'BAZ' : 'baz')
    },
}
