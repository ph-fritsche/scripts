module.exports = {
    description: 'Outputs "foo"',
    options: {
        loud: {description: 'UPPERCASE'},
    },
    run(params) {
        process.stdout.write(params.options.loud ? 'FOO' : 'foo')
    },
}
