const { stdout } = require('process')

module.exports = {
    description: 'Outputs "foo"',
    options: {
        loud: {description: 'UPPERCASE'},
    },
    run(params) {
        stdout.write(params.options.loud ? 'FOO' : 'foo')
    },
}
