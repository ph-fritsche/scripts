const { stdout } = require('process')

module.exports = {
    description: 'Outputs "bar"',
    options: {
        loud: {description: 'UPPERCASE'},
    },
    run(params) {
        stdout.write(params.options.loud ? 'BAR' : 'bar')
    },
}
