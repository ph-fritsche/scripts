module.exports = {
    description: 'Outputs "bar"',
    options: {
        loud: {description: 'UPPERCASE'},
    },
    run(params) {
        process.stdout.write(params.options.loud ? 'BAR' : 'bar')
    },
}
