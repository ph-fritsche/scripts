const { stdout } = require('process')

module.exports = {
    scripts: {
        'foobar': __dirname + '/foobar',
        'inline': {
            run: () => stdout.write('this is an inline script'),
        },
        'inline-require': require(__dirname + '/foobar'),
    },
}
